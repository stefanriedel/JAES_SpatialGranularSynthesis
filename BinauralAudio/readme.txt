Binaural audio files can be rendered to this folder by executing 'renderBinauralEvaluationStimuli.py'

In the script select any of the Flags and listen to the files with headphones.

"""
# Select parameter evaluation
EVAL_GrainLength = False
EVAL_MaxGrainDelay = False
EVAL_Layers = True

RENDER_DIFFUSE_REF = False # only enable once to render the ref. file
"""

To plot the evaluation figure as in the paper, execute 'plotBinauralEvaluation.py', after enabling / rendering every option once.

